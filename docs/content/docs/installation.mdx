---
title: Installation
description: Install Birch on your system
---

Birch can be installed in multiple ways depending on your platform and preferences.

## From Source

Build from source using Cargo:

```bash
git clone https://github.com/plyght/birch
cd birch
cargo build --release
sudo cp target/release/birch /usr/local/bin/
```

## Pre-built Binaries

Pre-built binaries may be available on the [releases page](https://github.com/plyght/birch/releases) for supported platforms.

## Docker

Build and run Birch in a container:

```bash
# Build the Docker image
docker build -t birch:latest .

# Run a command
docker run --rm birch:latest --help

# Run with environment variables
docker run --rm \
  -e VERCEL_TOKEN=your-token \
  -e VERCEL_PROJECT_ID=your-project \
  birch:latest rotate MY_API_KEY --env prod --service vercel
```

## Using Make

If you have the repository cloned:

```bash
make release    # Build release binary
make install    # Install to /usr/local/bin
make docker     # Build Docker image
```

## Verify Installation

Check that Birch is installed correctly:

```bash
birch --version
birch --help
```

You should see the version number and help text.

## Next Steps

After installation:

1. [Initialize configuration](/docs/configuration)
2. [Follow the Quick Start guide](/docs/quick-start)
3. [Set up provider credentials](/docs/connectors)

